<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/mini_player_root"
    android:layout_width="match_parent"
    android:layout_height="64dp"
        android:background="@color/phone_surface_elevated"
        android:clickable="true"
        android:elevation="8dp"
        android:focusable="true"
        android:gravity="center_vertical"
    android:minHeight="48dp"
    android:orientation="horizontal"
    android:paddingStart="8dp"
    android:paddingEnd="4dp">

    <ImageView
        android:id="@+id/mini_player_thumb"
        android:layout_width="96dp"
        android:layout_height="54dp"
        android:background="@color/phone_surface"
        android:contentDescription="@string/mini_player"
        android:scaleType="centerCrop" />

    <TextView
        android:id="@+id/mini_player_title"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_marginStart="12dp"
        android:layout_weight="1"
        android:ellipsize="end"
        android:maxLines="2"
        android:textColor="@color/phone_on_surface"
        android:textSize="13sp" />

    <ImageButton
        android:id="@+id/mini_player_play"
        android:layout_width="48dp"
        android:layout_height="48dp"
        android:background="?attr/selectableItemBackgroundBorderless"
        android:contentDescription="@string/mini_player_play"
        android:src="@drawable/ic_pause" />

    <ImageButton
        android:id="@+id/mini_player_close"
        android:layout_width="48dp"
        android:layout_height="48dp"
        android:background="?attr/selectableItemBackgroundBorderless"
        android:contentDescription="@string/mini_player_close"
        android:src="@drawable/ic_close" />
</LinearLayout>
